from flask_restful import fields

temp_fields = {
    'id': fields.Integer,
    'value': fields.String,
    'place': fields.String,
    'creation_date': fields.DateTime
}

temps_fields = {
    'id': fields.Integer,
    'value': fields.String,
    'place': fields.String,
    'creation_date': fields.DateTime
}

light_fields = {
    'id': fields.Integer,
    'value': fields.String,
    'place': fields.String,
    'creation_date': fields.DateTime
}

lights_fields = {
    'id': fields.Integer,
    'value': fields.String,
    'place': fields.String,
    'creation_date': fields.DateTime
}
